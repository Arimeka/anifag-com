= form_for [:backend, :videos, @article], html: {class: 'form-horizontal'} do |form|
  %fieldset
    %ul.nav.nav-tabs.tabs
      %li.active
        %a{href:'#tab1', 'data-toggle' => 'tab'}= t('label.form.main')
      %li
        %a{href:'#tab2', 'data-toggle' => 'tab'}= t('label.form.media')
      %li
        %a{href:'#tab3', 'data-toggle' => 'tab'}= t('label.form.seo')
    .tab-content
      #tab1.tab-pane.active= render '/backend/articles/tab1', form: form
      #tab2.tab-pane= render 'tab2', form: form
      #tab3.tab-pane= render '/backend/articles/tab3', form: form
    .row
      .col-sm-12
        .well
          = form.submit t('form.save'), data: { disable_with: t('form.wait') }, class: 'btn btn-success'
          = form.submit t('form.save_and_exit'), data: { disable_with: t('form.wait') }, class: 'btn btn-primary'
          = link_to t('form.cancel'), backend_videos_articles_path, class: 'btn btn-warning'
          - unless @article.new_record?
            = link_to t('form.delete'), backend_videos_article_path(@article), method: :delete, data: { confirm: t('form.sure?') }, class: 'btn btn-danger pull-right'
= tinymce
