- content_for :title, "#{t('activerecord.models.banner.labels.edit')} #{t('activerecord.models.banner.accusative')} - Anime Fag"

= render 'subnav'

%h1.page-header= "#{t('activerecord.models.banner.labels.edit')} #{t('activerecord.models.banner.accusative')}"

= render 'form'
